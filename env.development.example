######################################################################
# Basic application settings
######################################################################

# Application environment mode (development, production, or test)
# Affects logging format and other environment-specific behaviors
NODE_ENV=development

# Host address to bind the HTTP server to
# Use 'localhost' for local development, '0.0.0.0' for Docker/production
LISTEN_HOST=localhost

# Port number for the HTTP server
# Must be between 1 and 65535
LISTEN_PORT=8080

# Base path for API endpoints (without leading/trailing slashes)
# Will be combined with version to form the global prefix (e.g., /api/v1)
API_BASE_PATH=api

# Logging level for Pino logger
# Options: trace, debug, info, warn, error, fatal, silent
# Use 'debug' for development, 'warn' or 'error' for production
LOG_LEVEL=debug

# Timezone for the application
# Use UTC for consistency across environments
TZ=UTC

######################################################################
# Configuration file path
######################################################################

# Path to the YAML configuration file
# Can be absolute or relative to the project root
CONFIG_PATH=./config.yaml


######################################################################
# Custom Variables
# You can define your own variables here and use them in config.yaml
# Example: MY_TELEGRAM_TOKEN=...
######################################################################

